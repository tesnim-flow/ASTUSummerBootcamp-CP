class Solution(object):
    def intersection(self, nums1, nums2):
        s1 = set(nums1)
        result = []

        for num in set(nums2):
            if num in s1:
                result.append(num)

        return result
